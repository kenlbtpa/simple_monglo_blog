<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Mongo Blog</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <% if @user.nil?  %>
        <%# Logged Out %>
        <form class="navbar-form navbar-right">
          <button type="button" class="btn btn-success" data-toggle="modal" data-target="#loginModal" >Sign in</button>
          <button type="button" class="btn btn-info" data-toggle="modal" data-target="#registerModal">Register</button>
        </form>
      <% else %>
      <%# Logged In %>
        <form class="navbar-form navbar-right" action="user/destroy" >
          <button type="button" class="btn btn-success" data-toggle="modal" data-target="#blogModal" >Create Blog</button>
          <button type="submit" class="btn btn-danger"  >Logout</button>
        </form>
      <% end %>      
    </div><!--/.navbar-collapse -->
  </div>
</nav>

<section id="navModalForms">

  <% if @user.nil? %>
    <%= render "header/loginForm" %>
    <%= render "header/registerForm" %>
  <% else %>
    <%= render "header/blogForm" %>
  <% end %>

</section>




